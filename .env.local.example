# Anthropic — for the coach chat responses
ANTHROPIC_API_KEY=sk-ant-...

# OpenAI — for generating embeddings (query-time retrieval in the edge function)
OPENAI_API_KEY=sk-...

# Supabase marsbot project
# URL: https://loygyzhukfxdztkwfimr.supabase.co
# Keys: Supabase dashboard → Project Settings → API
SUPABASE_URL=https://loygyzhukfxdztkwfimr.supabase.co
SUPABASE_ANON_KEY=eyJhbGci...   # "anon" key — safe to expose in frontend
SUPABASE_SERVICE_ROLE_KEY=eyJhbGci...  # service role — NEVER expose, only for indexing script

# ──────────────────────────────────────────────────────────────────────────────
# VERCEL DEPLOYMENT: add ANTHROPIC_API_KEY, OPENAI_API_KEY, SUPABASE_URL,
# SUPABASE_ANON_KEY to Vercel project settings → Environment Variables.
# (SUPABASE_SERVICE_ROLE_KEY is only needed locally for the indexing script.)
# ──────────────────────────────────────────────────────────────────────────────

# LOCAL INDEXING SCRIPT:
# Run:  npm run index-knowledge
# This populates the coach_knowledge table in Supabase.
# Re-run any time project documents are updated.
